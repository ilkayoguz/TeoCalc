namespace TeoCalc.Rendering;

/// <summary>Silk / ImGui shell entry point (stub until HP Calc Explorer UI lands).</summary>
public static class CalcAppHost
{
  public static int Run()
  {
    // Future: ImGui window, model picker, calculator faceplate.
    return 0;
  }
}
